package fyi.allme.allus.companydata;

/** The picks no longer fit the current inputs or each other: clear them and pick again. */
public record PluginPicksInvalid() implements PluginOutputsResult {
}
